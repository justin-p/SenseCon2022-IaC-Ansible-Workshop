# do not edit this file. use defaults/main.yml to set variables.
_terraformproviders:
  aws:
    path: "{{ playbook_dir }}/terraform/aws"
  digitalocean:
    path: "{{ playbook_dir }}/terraform/digitalocean"
  hetzner:
    path: "{{ playbook_dir }}/terraform/hetzner"